# 血缘数据流转全链路说明

## 一、整体架构概览

本项目采用前后端分离架构，后端基于 Spring Boot + JPA 构建数据血缘服务，前端基于 Vue3 + ECharts 实现血缘图谱可视化。完整数据流经历 **数据库存储 → 后端实体 → DTO转换 → HTTP传输 → 前端API封装 → ECharts配置渲染** 六个主要阶段。

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  数据库存储层   │────▶│  后端服务层     │────▶│  前端渲染层     │
│  (MySQL表)      │     │  (Spring Boot)  │     │  (Vue+ECharts)  │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

---

## 二、数据流路径详解

### 2.1 第一层：数据库存储层

**核心表结构**：

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `data_source` | 数据源表 | id, name, type, host, port, database |
| `data_table` | 数据表元信息 | id, source_id, table_name, table_comment |
| `data_column` | 数据列元信息 | id, table_id, column_name, data_type |
| `data_lineage` | 血缘关系表 | id, source_table_id, target_table_id, lineage_type, transform_rule |

**关键关联关系**：
- `data_table.source_id` → `data_source.id`（多对一）
- `data_column.table_id` → `data_table.id`（多对一）
- `data_lineage.source_table_id` / `target_table_id` → `data_table.id`（多对一）

**代码位置**：[init.sql](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/sql/init.sql#L48-L54)

---

### 2.2 第二层：后端实体层（JPA Entity）

#### 2.2.1 核心实体类

**DataLineage 实体** —— 血缘关系实体

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键 |
| sourceTableId | Long | 源表ID |
| targetTableId | Long | 目标表ID |
| sourceColumnId | Long | 源列ID（可选） |
| targetColumnId | Long | 目标列ID（可选） |
| lineageType | String | 血缘类型: TABLE / COLUMN |
| transformRule | String | 转换规则 |
| description | String | 描述 |

**代码位置**：[DataLineage.java](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/backend/src/main/java/com/lineage/entity/DataLineage.java)

**DataTable 实体** —— 数据表实体

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键 |
| sourceId | Long | 关联数据源ID |
| tableName | String | 表名 |
| tableComment | String | 表注释 |

**代码位置**：[DataTable.java](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/backend/src/main/java/com/lineage/entity/DataTable.java)

**DataSource 实体** —— 数据源实体

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键 |
| name | String | 数据源名称 |
| type | String | 数据源类型 |

**代码位置**：[DataSource.java](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/backend/src/main/java/com/lineage/entity/DataSource.java)

#### 2.2.2 Repository 数据访问层

**DataLineageRepository** 提供三个核心查询方法：

- `findBySourceTableId(Long)` —— 查询某表作为源表的血缘
- `findByTargetTableId(Long)` —— 查询某表作为目标表的血缘
- `findAllByTableId(Long)` —— 查询表的所有血缘（上游+下游）

**代码位置**：[DataLineageRepository.java](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/backend/src/main/java/com/lineage/repository/DataLineageRepository.java)

---

### 2.3 第三层：服务转换层（关键转换节点①）

**LineageService.getLineageGraph(tableId)** 是后端数据转换的核心方法，完成 **数据库实体 → 图谱DTO** 的转换。

#### 转换流程：

```
输入: tableId (Long)
  │
  ▼
1. 查询血缘关系列表 findAllByTableId(tableId)
  │  得到 List<DataLineage>
  ▼
2. 收集所有相关表ID（源表+目标表+当前表）
  │  得到 Set<Long> tableIds
  ▼
3. 批量查询表信息 findAllById(tableIds)
  │  得到 Map<Long, DataTable> tableMap
  ▼
4. 收集数据源ID并查询
  │  得到 Map<Long, DataSource> sourceMap
  ▼
5. 构建节点列表 List<GraphNode>
  │  • id: "table_" + table.getId()
  │  • label: table.getTableName()
  │  • type: "table"
  │  • sourceName: source.getName()
  ▼
6. 构建边列表 List<GraphEdge>
  │  • id: "lineage_" + lineage.getId()
  │  • source: "table_" + lineage.getSourceTableId()
  │  • target: "table_" + lineage.getTargetTableId()
  │  • label: lineage.getTransformRule()
  ▼
输出: LineageGraphDTO(nodes, edges)
```

**代码位置**：[LineageService.java](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/backend/src/main/java/com/lineage/service/LineageService.java#L71-L128)

---

### 2.4 第四层：DTO 传输层

**LineageGraphDTO** —— 血缘图谱数据传输对象，是前后端交互的契约。

```java
public class LineageGraphDTO {
    private List<GraphNode> nodes;  // 节点列表
    private List<GraphEdge> edges;  // 边列表

    public static class GraphNode {
        private String id;          // 节点唯一标识
        private String label;       // 节点显示名称
        private String type;        // 节点类型: table / column
        private Long tableId;       // 关联表ID
        private Long columnId;      // 关联列ID
        private String sourceName;  // 数据源名称
    }

    public static class GraphEdge {
        private String id;          // 边唯一标识
        private String source;      // 源节点ID
        private String target;      // 目标节点ID
        private String label;       // 边标签（转换规则）
        private String lineageType; // 血缘类型
    }
}
```

**代码位置**：[LineageGraphDTO.java](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/backend/src/main/java/com/lineage/dto/LineageGraphDTO.java)

**Result<T>** —— 统一响应包装

```java
public class Result<T> {
    private Integer code;     // 状态码，200表示成功
    private String message;   // 提示信息
    private T data;           // 业务数据
}
```

**代码位置**：[Result.java](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/backend/src/main/java/com/lineage/dto/Result.java)

---

### 2.5 第五层：Controller API 层

**LineageController** 提供两个图谱相关接口：

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/lineage/graph/{tableId}` | GET | 根据表ID获取血缘图谱 |
| `/api/lineage/search` | GET | 根据表名搜索血缘图谱 |

**代码位置**：[LineageController.java](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/backend/src/main/java/com/lineage/controller/LineageController.java)

---

### 2.6 第六层：HTTP 传输层

#### 2.6.1 请求封装（前端）

**request.js** —— Axios 实例封装

- baseURL: `/api`
- timeout: 10000ms
- 请求拦截器：自动添加 `Authorization: Bearer {token}`
- 响应拦截器：自动解析 `Result.data`，code != 200 时统一报错

**代码位置**：[request.js](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/frontend/src/utils/request.js)

#### 2.6.2 API 接口定义

**api/index.js** 中定义的血缘相关接口：

```javascript
export function getAllLineages()          // GET /lineage
export function createLineage(data)       // POST /lineage
export function deleteLineage(id)         // DELETE /lineage/{id}
export function getLineageGraph(tableId)  // GET /lineage/graph/{tableId}
export function searchLineage(tableName)  // GET /lineage/search?tableName=xxx
```

**代码位置**：[api/index.js](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/frontend/src/api/index.js)

---

### 2.7 第七层：前端 ECharts 渲染层（关键转换节点②）

**Lineage.vue** 中的 `chartOption` computed 属性完成 **DTO数据 → ECharts配置** 的第二次关键转换。

#### 转换流程：

```
输入: graphData { nodes: [], edges: [] }
  │
  ▼
1. 节点数据转换 graphData.nodes.map()
  │  • id: node.id                → 保留
  │  • name: node.label           → ECharts节点名称
  │  • sourceName: node.sourceName → 自定义属性（tooltip用）
  │  • symbolSize: 50             → 节点大小
  │  • itemStyle.color: '#5470c6' → 节点颜色
  │  • label.show: true           → 显示标签
  ▼
2. 边数据转换 graphData.edges.map()
  │  • source: edge.source        → 源节点ID
  │  • target: edge.target        → 目标节点ID
  │  • label.formatter: edge.label → 边标签
  │  • lineStyle.color: '#91cc75' → 边线颜色
  │  • lineStyle.curveness: 0.2   → 边曲率
  ▼
3. 力导向布局配置
  │  • layout: 'force'            → 力导向布局
  │  • force.repulsion: 300       → 节点斥力
  │  • force.edgeLength: 150      → 边长度
  │  • roam: true                 → 开启缩放平移
  ▼
输出: ECharts option 对象
```

**代码位置**：[Lineage.vue](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/frontend/src/views/Lineage.vue#L168-L222)

#### ECharts 配置关键参数：

| 配置项 | 值 | 说明 |
|--------|----|------|
| series.type | 'graph' | 图谱类型 |
| series.layout | 'force' | 力导向布局 |
| series.force.repulsion | 300 | 节点间斥力 |
| series.force.edgeLength | 150 | 边的长度 |
| series.roam | true | 开启鼠标缩放和平移 |
| series.emphasis.focus | 'adjacency' | 高亮相邻节点 |
| tooltip.trigger | 'item' | 悬浮提示 |

---

## 三、两次关键数据转换对比

| 转换节点 | 位置 | 输入 | 输出 | 核心职责 |
|----------|------|------|------|----------|
| **转换①** | 后端 `LineageService.getLineageGraph()` | `DataLineage` + `DataTable` + `DataSource` 实体 | `LineageGraphDTO` | 业务数据组装，关联查询，ID前缀化 |
| **转换②** | 前端 `chartOption` computed | `LineageGraphDTO` 结构数据 | ECharts `option` 配置对象 | 可视化样式映射，布局配置，交互配置 |

---

## 四、完整调用链路时序

```
用户搜索表名
    │
    ▼
Lineage.vue handleSearch()
    │  调用 searchLineage(tableName)
    ▼
api/index.js searchLineage()
    │  GET /api/lineage/search?tableName=xxx
    ▼
前端 request.js 拦截器
    │  添加 Authorization header
    ▼
后端 LineageController.searchLineage()
    │
    ▼
LineageService.searchLineage()
    │  1. 按表名模糊查询 DataTable
    │  2. 调用 getLineageGraph(tableId)
    ▼
LineageService.getLineageGraph()
    │  1. findAllByTableId() 查询血缘
    │  2. 批量查询表信息
    │  3. 批量查询数据源信息
    │  4. 构建 GraphNode 列表
    │  5. 构建 GraphEdge 列表
    │  6. 返回 LineageGraphDTO
    ▼
Result<LineageGraphDTO> 包装
    │
    ▼
HTTP 响应 JSON
    │
    ▼
前端 request.js 响应拦截器
    │  解析 res.data
    ▼
Lineage.vue graphData 赋值
    │
    ▼
chartOption computed 重新计算
    │  1. 节点数据映射（样式、标签）
    │  2. 边数据映射（颜色、曲率）
    │  3. 力导向布局配置
    ▼
VChart 组件接收 option
    │
    ▼
ECharts 渲染图谱
```

---

## 五、关键代码索引

| 层级 | 文件 | 关键行 |
|------|------|--------|
| 实体层 | [DataLineage.java](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/backend/src/main/java/com/lineage/entity/DataLineage.java) | 全文件 |
| Repository | [DataLineageRepository.java](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/backend/src/main/java/com/lineage/repository/DataLineageRepository.java) | L30-L31 findAllByTableId |
| **转换①** | [LineageService.java](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/backend/src/main/java/com/lineage/service/LineageService.java) | L71-L128 getLineageGraph |
| DTO | [LineageGraphDTO.java](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/backend/src/main/java/com/lineage/dto/LineageGraphDTO.java) | 全文件 |
| Controller | [LineageController.java](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/backend/src/main/java/com/lineage/controller/LineageController.java) | L49-L61 图谱接口 |
| API封装 | [api/index.js](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/frontend/src/api/index.js) | L184-L231 血缘API |
| 请求封装 | [request.js](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/frontend/src/utils/request.js) | L27-L36 响应拦截器 |
| **转换②** | [Lineage.vue](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/frontend/src/views/Lineage.vue) | L168-L222 chartOption |
| 渲染入口 | [Lineage.vue](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/frontend/src/views/Lineage.vue) | L27-L33 VChart组件 |
| 初始化数据 | [init.sql](file:///Users/tog/Desktop/code/gsb/B-595/B-595_winter/sql/init.sql) | L48-L54 示例血缘数据 |

---

## 六、数据结构对照总表

### 节点数据流转对照

| 阶段 | 唯一标识 | 显示名称 | 类型字段 | 附加信息 |
|------|----------|----------|----------|----------|
| 数据库 | `data_table.id` (Long) | `data_table.table_name` | - | `source_id` |
| JPA实体 | `DataTable.id` (Long) | `DataTable.tableName` | - | `DataTable.sourceId` |
| GraphNode DTO | `GraphNode.id` = `"table_" + id` (String) | `GraphNode.label` | `GraphNode.type` = `"table"` | `GraphNode.sourceName` |
| ECharts节点 | `option.series[0].data[i].id` | `option.series[0].data[i].name` | - | `sourceName` (自定义属性) |

### 边数据流转对照

| 阶段 | 唯一标识 | 源端 | 目标端 | 标签 |
|------|----------|------|--------|------|
| 数据库 | `data_lineage.id` (Long) | `source_table_id` (Long) | `target_table_id` (Long) | `transform_rule` |
| JPA实体 | `DataLineage.id` (Long) | `DataLineage.sourceTableId` | `DataLineage.targetTableId` | `DataLineage.transformRule` |
| GraphEdge DTO | `GraphEdge.id` = `"lineage_" + id` | `GraphEdge.source` = `"table_" + sourceId` | `GraphEdge.target` = `"table_" + targetId` | `GraphEdge.label` |
| ECharts边 | - | `option.series[0].edges[i].source` | `option.series[0].edges[i].target` | `edge.label.formatter` |
